# Dependencias de PRODUCCIÓN (Docker/Fly.io).
# Lista curada: solo lo que importa el backend en runtime.
# Las dependencias transitivas las resuelve pip de forma limpia
# (el antiguo volcado completo de pip freeze incluía CLIs de desarrollo,
# p.ej. kimi-cli/fastmcp/mcp, que exigían uvicorn>=0.35 y
# pydantic-settings>=2.5.2 y rompían la resolución).

# Núcleo API
fastapi==0.111.0
starlette==0.37.2
uvicorn==0.30.1
python-multipart==0.0.9

# Configuración / validación
pydantic==2.13.4
pydantic-settings==2.3.4
python-dotenv==1.1.0

# Base de datos
SQLAlchemy==2.0.31
alembic==1.13.2
psycopg[binary]==3.3.4

# Auth
passlib==1.7.4
bcrypt==4.0.1
PyJWT==2.8.0

# Ajedrez e IA
chess==1.10.0
google-generativeai==0.7.2

# Audio (finales teóricos)
edge-tts==7.2.8
gTTS==2.5.4
